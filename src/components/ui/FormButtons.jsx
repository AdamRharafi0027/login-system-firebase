
const FormButtons = ({text}) => {
  return (
    <>
        <button type="submit">
            <span className="text">{text}</span>
            <span>Now!</span>
          </button>
    </>
  )
}

export default FormButtons